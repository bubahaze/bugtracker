spring:
    mail:
        password: root
        username: admin
        host: localhost
        port: 3025 # default protocol port + 3000 as offset-->
        protocol: smtp
        test-connection: true